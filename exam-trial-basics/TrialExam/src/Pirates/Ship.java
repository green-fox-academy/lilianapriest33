/*package Pirates;

import java.util.ArrayList;
import java.util.List;

public class Ship {
    protected Pirate captain;
    protected Pirate pirate = new ArrayList<>();

    public Ship() {
    }

    //getters/setters
    public void setCaptain(Pirate captain) {
        this.captain = captain;
    }

    public void setPirate(Pirate pirate) {
        this.pirate = pirate;
    }

    //methods

    public List<Pirate> getPoorPirates() {
        return crew;
    }

    public int getGolds() {
        return goldAmount;
    }
}
getPoorPirates() which returns a list of names containing the pirates that
have a wooden leg and have less than 15 golds

getGolds() which returns the sum of gold owned by the pirates of that particular ship

lastDayOnTheShip() which calls the pirates' party() method.

prepareForBattle() which calls the pirates' work() method 5 times
then the ship's lastDayOnTheShip() method.
*/
